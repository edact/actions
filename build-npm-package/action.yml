name: "build npm package"
description: "builds npm package"
author: "eDACT"
inputs:
  package_version:
    description: "version the package should get"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"
